import 'package:flutter/material.dart';

class FakeLoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Login'), // Just use a word that exists on the real screen
      ),
    );
  }
}